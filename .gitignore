.build/
.swiftpm/
.DS_Store
*.xcodeproj
!ios/KumoneIOS.xcodeproj
xcuserdata/
DerivedData/

# Release artifacts (the IPA the release workflow zips). Never tracked: these are
# build output, and a committed IPA goes stale the moment the source changes —
# a stale CarPlay IPA in particular looks installable but predates the fixes.
dist/

# Generated by Scripts/configure-ios.sh --carplay (opt-in CarPlay overlay)
ios/Config/CarPlay.local.xcconfig
ios/Config/Generated/
